<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<style>
    @media (max-width: 576px) {
        
        .list-group-item span,
        .list-group-item div a,
        .list-group-item .badge {
            font-size: 12px; 
            margin-bottom: 10px;

        }

        .btn {
            font-size: 12px; 
            padding: 10px 8px; 
            margin-bottom: 20px;

        }

        .list-group-item {
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .list-group-item img {
            width: 150px;
            height: 200px;
        }
    }

.btn{
                margin-bottom: 20px;

}

</style>
<head>
<body>
{{# if this.products }}
<div class="container-fluid px-0"> 
    <div class="row mt-5">
        <div class="col-12"> 
            <ul class="list-group">
                {{#each products}}
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <img src="{{this.item.imagePath}}">
                    <span style="font-weight: bold">{{this.item.title}}</span>
                    <span class="badge badge-success">${{this.price}}</span>
                    <div>
                        <a href="/reduce/{{this.item._id}}" class="btn btn-secondary mr-1">-</a>
                        <a href="/remove/{{this.item._id}}" class="btn btn-secondary mr-1">Eliminar Todos</a>
                        <a href="/add/{{this.item._id}}" class="btn btn-secondary">+</a>
                    </div>
                    <span class="badge badge-primary badge-pill">{{this.qty}}</span>
                </li>
                {{/each}}
            </ul>
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-12"> 
            <strong>Total: {{totalPrice}}</strong>
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-12"> 
            <hr>
            <a href="/vaciar" type="button" class="btn btn-danger mr-2">Vaciar Carrito</a>
            <a href="/checkout" type="button" class="btn btn-success mr-2">Realizar Compra</a>
            <a href="/" type="button" class="btn btn-primary">Seguir Comprando</a>
        </div>
    </div>
</div>
{{else}}
<div class="container-fluid px-0"> 
    <div class="row justify-content-center align-items-center" style="height: 100vh;"> 
        <div class="col-12 text-center">
            <h2 class="text-lg text-xl">CARRO VACÍO</h2> 
            <div class="mt-3">
                <a href="/" type="button" class="btn btn-primary">Regresar A Comprar</a>
            </div>
        </div>
    </div>
</div>
{{/if}}
</body>
</html>
