<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Producto</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            box-sizing: border-box;
        }
        h1 {
            text-align: center;
        }
        form {
            max-width: 500px;
            margin: 0 auto;
        }
        label {
            display: block;
            margin-bottom: 5px;
        }
        input[type="text"],
        input[type="number"],
        textarea {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }
        button[type="submit"],
        button[type="button"] {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: calc(50% - 5px); 
        }
        button.blue {
            background-color: #007bff;
            color: white;
        }
        button.red {
            background-color: #f44336;
            color: white;
        }
        button[type="submit"]:hover,
        button[type="button"]:hover,
        button.blue:hover,
        button.red:hover {
            opacity: 0.8;
        }
        .button-container {
            display: flex;
            justify-content: space-between; 
            margin-top: 20px; 
        }
        @media only screen and (max-width: 600px) {
            input[type="text"],
            input[type="number"],
            textarea {
                width: calc(100% - 16px);
            }
            button[type="submit"],
            button[type="button"],
            button.blue,
            button.red {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <h1>Editar Producto</h1>
    <form action="/producto/editar-producto/{{producto._id}}" method="POST">
        <label for="imagePath">Enlace Imagen:</label>
        <input type="text" name="imagePath" id="imagePath" value="{{producto.imagePath}}" required><br>
        <label for="title">Título:</label>
        <input type="text" name="title" id="title" value="{{producto.title}}" required><br>
        <label for="description">Descripción:</label>
        <textarea name="description" id="description" required>{{producto.description}}</textarea><br>
        <label for="price">Precio:</label>
        <input type="number" name="price" id="price" value="{{producto.price}}" required><br>
        <div class="button-container">
        <button type="button" class="red" onclick="window.location.href='/producto/insertar-producto'">Regresar a Productos</button>
            <button type="submit" class="blue">Guardar Cambios</button>
        </div>
    </form>
</body>
</html>
